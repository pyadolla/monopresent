{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        { 
            "label": "echo workspace folder",
            "type": "shell",
            "command": "echo ${workspaceFolder}"  // Use 'echo' or 'print' depending on your OS
        }
    ]
}